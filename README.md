Hola, este es el readme, aca tienen el codigo sql, pronto añadire mas contenido










PSQL
    -- 1. Crear la tabla de productos / catálogo
CREATE TABLE IF NOT EXISTS public.products (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL,
    name TEXT NOT NULL,
    description TEXT,
    price NUMERIC NOT NULL DEFAULT 0,
    stock INT NOT NULL DEFAULT 0
);

-- 2. Desactivar temporalmente RLS para que el frontend pueda leer los datos en local sin trabas
ALTER TABLE public.products DISABLE ROW LEVEL SECURITY;

-- 3. Limpiar datos viejos por las dudas
TRUNCATE TABLE public.products;

-- 4. Insertar un par de productos de prueba para ver el catálogo funcionando ya mismo
INSERT INTO public.products (name, description, price, stock)
VALUES 
('Jeans American Street Style', 'Denim rígido premium, corte holgado vintage.', 45000, 12),
('Remera Boxy Fit Black', 'Algodón pesado 24/1, cuello grueso ideal para uso diario.', 22000, 25),
('Campera Vintage Denim', 'Corte oversize con lavado clásico de los 90.', 68000, 8);
